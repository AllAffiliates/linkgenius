function thisIsADummy() {
    // Because otherwhise Typescript would complain that there are no sources.
    // Typescript is needed because otherwise VSCode will not understand which node module are used
    // Without typescript vscode will look in its own directory for modules
}